declare namespace enums {
    enum tipoDocumento {DNI, LC, LE}

    enum referencia {particular, laboral, vecino, familiar}

    enum tipoTrabajo {relacionDependencia, actividadComercial}

    enum estadoCivil {soltero, casado, divorciado, concubino, separado}

    enum tipoContratacion {contratado, noContratado}

    enum tipoIngreso {semanal, quincenal, mensual}

    enum sexo {F, M}

    enum categoriaActividadComercial {B, C, D, E, F, G, H, I, J, K, L}
}